
#pragma once
#include <iostream>
#include <locale>

namespace Varparm 
{
	int ivarparm(int n, ...);
	int svarparm(short n, ...);
	double fvarparm(float n, ...);
	double dvarparm(double n, ...);
}